'use strict';

module.exports = ( config ) => {
    console.log( '---------------------------- Config ----------------------------' );
    console.log( JSON.stringify( config ) );
    console.log( '----------------------------------------------------------------' );
};
